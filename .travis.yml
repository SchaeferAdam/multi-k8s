sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -adamschaefer/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=truadamschaefer/react-test npm test

after_success:
  - docker build -adamschaefer/multi-client ./client
  - docker build -adamschaefer/multi-nginx ./nginx
  - docker build -adamschaefer/multi-server ./server
  - docker build -adamschaefer/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push adamschaefer/multi-client
  - docker push adamschaefer/multi-nginx
  - docker push adamschaefer/multi-server
  - docker push adamschaefer/multi-worker
